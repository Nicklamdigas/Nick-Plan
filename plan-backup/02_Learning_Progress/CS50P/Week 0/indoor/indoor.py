#problem is that when running the python file and user types something in capital letters 
# the output should be the same but in small letters
# so the program should first have textbox where input in capital letters can be made by the user
# the program is to copy the input and change it to small letters then output that #
user = input()
print(user.lower())